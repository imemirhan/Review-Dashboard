// Review categories used across dashboard and property pages
export const REVIEW_CATEGORIES = ['Cleanliness', 'Communication', 'Value'];

// Theme colors (used for consistent styling)
export const THEME = {
  primary: '#164f4c',
  accent: '#2563eb', // blue
};

// Default limits or thresholds
export const DEFAULT_IMAGE_URL = 'https://picsum.photos/';
export const DEFAULT_MIN_RATING = 0;
export const DEFAULT_MAX_RATING = 5;
export const DEFAULT_SORT_ORDER = 'desc';
